import Box  from "@mui/material/Box";
import styled from "@emotion/styled";

export const BoxContainerNotifications = styled(Box)({
    margin: "24px"
})

export const BoxNotification = styled(Box)({
    marginY: 3
})