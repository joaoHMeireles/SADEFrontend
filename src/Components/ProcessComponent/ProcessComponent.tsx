

export default function ProcessComponent(props: {titulo: string, tamanho: string, solicitante: string, status: string, tipo: string}){
    return(
        <div>
            aaaa
        </div>
    )
}